---
interface ProcessStep {
  number: number;
  title: string;
  description: string;
}

const steps: ProcessStep[] = [
  {
    number: 1,
    title: 'Análisis y Consultoría',
    description:
      'Conocemos tu negocio, objetivos y público. Escuchamos activamente para entender exactamente qué necesitas.',
  },
  {
    number: 2,
    title: 'Propuesta Personalizada',
    description:
      'Te presentamos un plan detallado con funcionalidades, timeline y presupuesto justo. Sin sorpresas.',
  },
  {
    number: 3,
    title: 'Desarrollo Profesional',
    description:
      'Construimos tu proyecto con código moderno. Te mantenemos informado del avance cada paso del camino.',
  },
  {
    number: 4,
    title: 'Entrega y Soporte',
    description:
      'Lanzamos tu sitio con éxito. Te enseñamos a usarlo y ofrecemos soporte continuo para el futuro.',
  },
];
---

<section class="py-24 bg-white">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="section-heading">
        Nuestro proceso de trabajo
      </h2>
      <p class="section-subheading">
        De la idea al lanzamiento: 4 pasos simples
      </p>
    </div>

    <div class="relative">
      {/* Connecting line */}
      <div
        class="hidden md:block absolute top-1/4 left-0 right-0 h-1 bg-gradient-to-r from-blue-400 via-blue-600 to-blue-800"
      >
      </div>

      <div class="grid md:grid-cols-4 gap-8">
        {
          steps.map((step) => (
            <div class="relative">
              <div
                class="bg-gradient-to-br from-blue-600 to-blue-800 w-16 h-16 rounded-full flex items-center justify-center text-white font-bold text-2xl mx-auto mb-6 shadow-lg relative z-10"
              >
                {step.number}
              </div>
              <div class="bg-slate-50 p-6 rounded-xl">
                <h3 class="text-xl font-bold text-slate-900 mb-3 text-center">
                  {step.title}
                </h3>
                <p class="text-slate-600 text-center text-sm leading-relaxed">
                  {step.description}
                </p>
              </div>
            </div>
          ))
        }
      </div>
    </div>

    <div class="mt-16 text-center">
      <p class="text-lg text-slate-600">
        Cada proyecto es único. Adaptamos nuestro proceso a tus necesidades y tiempos.
      </p>
    </div>
  </div>
</section>
