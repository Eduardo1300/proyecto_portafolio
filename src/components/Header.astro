---
---

<header class="sticky top-0 z-50 bg-slate-950/95 backdrop-blur-sm border-b border-slate-800">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-4 flex items-center justify-between">
    {/* Logo */}
    <a href="/" class="text-2xl font-bold text-white">
      devstudio
    </a>

    {/* Navigation */}
    <nav class="hidden md:flex items-center gap-8">
      <a href="#servicios" class="text-slate-400 hover:text-white transition">
        Servicios
      </a>
      <a href="#proceso" class="text-slate-400 hover:text-white transition">
        Proceso
      </a>
      <a href="#beneficios" class="text-slate-400 hover:text-white transition">
        Beneficios
      </a>
      <a href="#faq" class="text-slate-400 hover:text-white transition">
        FAQ
      </a>
      <a
        href="#contacto"
        class="cta-primary"
      >
        Contactar
      </a>
    </nav>

    {/* Mobile menu button */}
    <button class="md:hidden text-white" id="mobile-menu">
      ☰
    </button>
  </div>

  {/* Mobile menu */}
  <nav
    id="mobile-nav"
    class="hidden md:hidden border-t border-slate-800 bg-slate-950"
  >
    <div class="px-4 py-4 space-y-4">
      <a
        href="#servicios"
        class="block text-slate-400 hover:text-white"
      >
        Servicios
      </a>
      <a
        href="#proceso"
        class="block text-slate-400 hover:text-white"
      >
        Proceso
      </a>
      <a
        href="#beneficios"
        class="block text-slate-400 hover:text-white"
      >
        Beneficios
      </a>
      <a
        href="#faq"
        class="block text-slate-400 hover:text-white"
      >
        FAQ
      </a>
      <a
        href="#contacto"
        class="block cta-primary text-center"
      >
        Contactar
      </a>
    </div>
  </nav>
</header>

<script>
  const mobileMenuBtn = document.getElementById('mobile-menu');
  const mobileNav = document.getElementById('mobile-nav');

  if (mobileMenuBtn && mobileNav) {
    mobileMenuBtn.addEventListener('click', () => {
      mobileNav.classList.toggle('hidden');
    });

    // Close menu when clicking a link
    const links = mobileNav.querySelectorAll('a');
    links.forEach((link) => {
      link.addEventListener('click', () => {
        mobileNav.classList.add('hidden');
      });
    });
  }
</script>
